import en from "./en";
import es from "./es";
import no from "./no";
import ru from "./ru";

const locales = {
	default: en.default,
	en: en,
	es: es,
	no: no,
    ru: ru,
};

export default locales;
